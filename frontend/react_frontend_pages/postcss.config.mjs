/**
 * PostCSS config file.
 *
 * Tailwind CSS v4 (via Vite) already sets up all required plugins,
 * so you don't need to add `tailwindcss` or `autoprefixer`.
 *
 * Use this file only if you want to add extra PostCSS plugins.
 */
export default {}